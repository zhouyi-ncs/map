现在需要用react做一个前端项目，技术站的话是用leaflet来做地图。地图有一个地图层级，地图层级是树形结构，以建筑，楼层，房间等级划分。楼层里面会有房间勾勒出来的区域数据。然后里面会有各种设备，设备这里面包括了机器人、摄像头和普通设备这三个大类，各个类别其中还有各个小类，即为具体的设备类型。好，然后在这里面的话，我们还会在这个图里面标注各个区域，因为这个图是根据建筑图的这种CAD的图来进行渲染的，这里是用的是像素坐标系，需要参考图片大小来决定。然后它的坐标点和各，就是区域的坐标点和设备的坐标点是需要规定的好，在这个这个地图组件的右上角是两个fix固定的筛选框。第一个筛选框的话，它是决定了这个当前有几个楼层，点击就可以切换到不同的楼层建筑图片。然后，不同图片里面，它的数据是根据对应的楼层去渲染的。另外一个筛选框是筛选这个设备的，点击选择后就只是前端单纯过滤当前楼层的设备数据，被过滤掉的设备不会被渲染展示。然后还有就是在各在这个图层中，每个不同的设备它会有对应的点击事件。这里先暂时的给出留出空位，后面再进行填补，注意各个设备状态不同，触发的事件不同。各个设备的状态你可以先自行模拟，需要根据不同设备来进行调整。这里的所有数据都需要进行默克处理，就比如building定的这个楼层数，它有建筑、楼层、房间，然后这是个树形结构的数据，嗯，还有设备的数据。设备数据是根据刚刚说的三大类的数据进行模拟，然后，他们各自的设备详情的数据也需要进行去模拟啊。现在能想到就是设备的话，就有设备ID、设备状态，还有设备名称。还有需要模拟的就是对应楼层的数据，里面需要标注不同的区域。还有一个很重要的交互逻辑，就是在各个建筑平面类，它需要根据这个树形结构来规定它不同区域之间会有一个点击关系，这个也是需要渲染在建筑地图上的，就比如楼层A它里面会有一个房间A，然后这个房间A是会被区域划出来的。然后在这个区域里面，它会有一个点击按钮。点击之后，它会直接跳转进这个房间A的这个平面图里面去，与之相应的房间A的数据也要一同更新意思就是说，这个整个地图的，无论是切换的地图的这个层数，还是设备数据都是需要进行切换的，根据当前对应的地图。

这里所有需要交互的数据，需要用前端模拟。目前没有接口，所以说要以前端的形式来模拟接口的实现，并且把这些模拟的接口输出成一个文档，记录为一个to do, 后面好以此来更新接口。可以先梳理一下前端需要用到哪些接口，以及这里有图片，图片的名称代表了图片的这个具体的这个用法，然后你可以参考图片进行实现这个地图项目，需要注意的是，名称叫map的这个PNG图片，它就是一个地图的地图图片，你不用去看它里面有什么内容，就只用它来当做渲染地图的底图就可以了。只需要注意去拿到它的对应的宽高，以此来作为当前地图的像素坐标点的基础数据。这里有的东西是没有给到的，就比如说设备的icon，还有设备在不同状态下会渲染到的样式。这些话你可以自行去生成，实行计划前，先分析一下本次开发的开发计划。嗯，最主要的是头脑风暴一下，我所说的这次计划有哪些需要确认的点没有说到可以提出问题，最后再开始实行计划